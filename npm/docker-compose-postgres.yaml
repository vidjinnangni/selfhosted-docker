
services:
  app:
    image: jc21/nginx-proxy-manager:2.16.0@sha256:4393e642e233e5efd5adeb7f10918d4aaea71c9ed875b94190273fc8e87f9594
    container_name: nginx-proxy-manager
    restart: unless-stopped
    ports:
      - "${NPM_HTTP_PORT}:80"
      - "${NPM_HTTPS_PORT}:443"
      - "${NPM_UI_PORT}:81"
    environment:
      - DB_TYPE=postgres
      - DB_PG_HOST=postgres
      - DB_PG_PORT=5432
      - DB_PG_NAME=${POSTGRES_DB}
      - DB_PG_USER=${POSTGRES_USER}
      - DB_PG_PASSWORD=${POSTGRES_PASSWORD}
      - TZ=${TZ}
    volumes:
      - npm_data:/data
      - npm_letsencrypt:/etc/letsencrypt
    depends_on:
      - postgres
    networks:
      - proxy_network

  postgres:
    image: postgres:15@sha256:724292da1f2e50bdccfc3302ce75bbba7f4a6076701b588cc795fcac65683550
    container_name: nginx-proxy-manager-postgres
    restart: unless-stopped
    environment:
      - POSTGRES_DB=${POSTGRES_DB}
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
    volumes:
      - npm_postgres_data:/var/lib/postgresql/data
    networks:
      - proxy_network

volumes:
  npm_data:
  npm_letsencrypt:
  npm_postgres_data:

networks:
  proxy_network:
    driver: bridge